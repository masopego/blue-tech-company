import * as React from "react";
import Box from "@mui/material/Box";
import { text } from "./content/literals";

export const SocialResponsability: React.FC = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <p>{text}</p>
    </Box>
  );
};
